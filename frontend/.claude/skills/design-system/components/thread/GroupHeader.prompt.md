Group title, visibility and thread count — sits directly above `ThreadTabs`.

```jsx
<GroupHeader name="Der Erinnerungsmarkt" visibility="Privat" threadCount={6} threadLimit={12} />
```

Do not restate the visibility anywhere else on the page. Titles stay at Newsreader 400 — never bold
them.
