Bordered badge for group visibility and post state — never filled, never coloured.

```jsx
<Badge>Privat</Badge>
<Badge variant="tag">gemerkt</Badge>
<Badge variant="tag">3 neu</Badge>
```

Colour is never used to encode meaning here, so a badge cannot signal severity — it only names a
fact. Never put a bare number in one.
